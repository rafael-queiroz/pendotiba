server:
  port: 9092

eureka:
  instance:
    hostname: localhost
    port: 9091
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 3000

security:
    oauth2:
        client:
            access-token-validity-seconds: 1800
            authorized-grant-types: password,refresh_token
            client-id: angular
            client-secret: $2a$10$.VCJHY5owObsgQo93jbRv.sPB1RuSIx22ib00TlFy4Rkl6IMbdr7K
            resource-ids: resources
            scope: read,write

spring:
    datasource:
        data: classpath:/sql/data.sql
        driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
        password: 'Trixrio@'
        #platform: mysql
        schema: classpath:/sql/schema.sql
        url: jdbc:sqlserver://localhost;databaseName=pendotiba_logic_bus;sendTimeAsDateTime=false
        username: sa
    jpa:
        database-platform: org.hibernate.dialect.SQLServer2012Dialect
        generate-ddl: false
        hibernate:
            ddl-auto: none
        show-sql: true
